version: v2
modules:
  - path: .
    excludes:
      - bin
      - .git
    name: buf.build/fernet/proto-cli
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
deps:
  - buf.build/googleapis/googleapis
